# 강산재 홈페이지 배포 가이드

GitHub에 올리고 Vercel로 띄우는 전 과정입니다. 처음 하셔도 30분이면 끝납니다.
개발 도구를 설치하지 않고 **브라우저만으로** 진행하는 방법으로 안내합니다.

---

## 준비물

| 항목 | 비용 | 비고 |
|---|---|---|
| GitHub 계정 | 무료 | github.com |
| Vercel 계정 | 무료 | vercel.com — GitHub 계정으로 로그인 |
| 도메인 | 연 1~2만원 | 나중에 연결해도 됩니다 |

**도메인 없이도 바로 배포됩니다.** Vercel이 `강산재-xxx.vercel.app` 형태의 주소를 무료로 줍니다. 이 주소로 먼저 띄워 보시고, 마음에 들면 도메인을 붙이십시오.

---

## 1단계 — GitHub에 저장소 만들기

1. github.com 접속 후 로그인
2. 우측 상단 **`+`** → **New repository**
3. 아래처럼 입력

| 항목 | 입력값 |
|---|---|
| Repository name | `gangsanjae` |
| Description | 강산재 한옥호텔 공식 홈페이지 |
| 공개 범위 | **Private** 권장 (Public도 배포는 됩니다) |
| Initialize this repository | **아무것도 체크하지 마십시오** |

4. **Create repository** 클릭

> Private으로 해도 Vercel 배포와 홈페이지 공개에는 아무 지장이 없습니다. 소스 코드만 비공개일 뿐입니다.

---

## 2단계 — 파일 올리기

저장소가 만들어지면 나오는 화면에서 **uploading an existing file** 링크를 클릭하십시오.

1. 압축을 푼 폴더를 여십시오 (`gangsanjae-deploy`)
2. **폴더 자체가 아니라, 폴더 안의 내용물 전부**를 드래그해서 브라우저에 놓으십시오

올라가야 할 항목입니다.

```
index.html
space.html
healing.html
info.html
404.html
favicon.svg
robots.txt
sitemap.xml
vercel.json
.gitignore
assets/          ← 폴더 통째로
```

3. 아래 **Commit changes** 버튼 클릭

> **주의** — `assets` 폴더 안에 사진 44장이 들어 있습니다. 업로드에 2~3분 걸릴 수 있으니 창을 닫지 마십시오.
> 만약 `.gitignore` 파일이 안 보이면, 맥은 `Cmd + Shift + .` / 윈도우는 탐색기 보기 설정에서 숨김 파일 표시를 켜십시오. 없어도 배포에는 지장 없습니다.

---

## 3단계 — Vercel 연결

1. vercel.com 접속 → **Continue with GitHub** 으로 로그인
2. 상단 **Add New...** → **Project**
3. 방금 만든 `gangsanjae` 저장소 옆 **Import** 클릭
4. 설정 화면이 나오면 **아무것도 건드리지 말고** 그대로 두십시오

| 항목 | 값 |
|---|---|
| Framework Preset | Other |
| Build Command | 비워둠 |
| Output Directory | 비워둠 |

5. **Deploy** 클릭

1~2분 뒤 축하 화면과 함께 주소가 나옵니다. 눌러서 확인해 보십시오.

**여기까지가 배포 완료입니다.** 이제 이 주소는 전 세계에서 접속됩니다.

---

## 4단계 — 도메인 연결 (선택)

### 도메인 구입

| 추천 | 이유 |
|---|---|
| `gangsanjae.com` | 외국인 고객 대응, 가장 무난 |
| `gangsanjae.kr` | 국내 신뢰도, 가격 저렴 |

가비아, 후이즈, Cloudflare 어디서 사셔도 됩니다.

### 연결 방법

1. Vercel 프로젝트 → 상단 **Settings** → 좌측 **Domains**
2. 구입한 도메인 입력 → **Add**
3. 화면에 뜨는 값을 도메인 구입처의 DNS 설정에 입력

| 종류 | 이름 | 값 |
|---|---|---|
| A | @ | Vercel이 알려주는 IP |
| CNAME | www | Vercel이 알려주는 주소 |

4. 10분~1시간이면 연결됩니다. HTTPS 인증서는 Vercel이 자동으로 붙입니다.

---

## 5단계 — 주소 치환 (도메인 연결 후 필수)

현재는 Vercel 기본 주소인 `gangsanjae.vercel.app` 으로 채워져 있습니다.
나만의 도메인을 연결하면 아래 파일들에서 `gangsanjae.vercel.app` 을 실제 도메인으로 바꿔 주십시오.

| 파일 | 개수 |
|---|---|
| `robots.txt` | 1곳 |
| `sitemap.xml` | 4곳 |
| `index.html` · `space.html` · `healing.html` · `info.html` | 각 3~4곳 |

**GitHub 웹에서 바꾸는 방법**

1. 저장소에서 파일 클릭 → 연필 아이콘(Edit)
2. `Ctrl + F` 로 `gangsanjae.vercel.app` 찾기
3. 실제 도메인으로 바꾸기 (예: `gangsanjae.com`)
4. **Commit changes**

커밋하면 **Vercel이 자동으로 다시 배포합니다.** 1분 뒤 반영됩니다.

> 이 작업이 번거로우시면 도메인만 알려주십시오. 치환된 파일을 만들어 드리겠습니다.

---

## 6단계 — 검색엔진 등록

도메인 연결이 끝나면 반드시 하십시오. **이걸 안 하면 검색에 안 잡힙니다.**

### 네이버

1. [searchadvisor.naver.com](https://searchadvisor.naver.com) 접속
2. 웹마스터도구 → 사이트 등록 → 도메인 입력
3. 소유확인 → HTML 태그 방식 선택 → 나온 태그를 `index.html` 의 `<head>` 안에 붙여넣고 커밋
4. 확인 완료 후 → 요청 → **사이트맵 제출** → `sitemap.xml` 입력

### 구글

1. [search.google.com/search-console](https://search.google.com/search-console) 접속
2. 속성 추가 → 도메인 또는 URL 접두어 입력
3. 소유확인 후 → Sitemaps → `sitemap.xml` 제출

### 그 다음

- 네이버 스마트플레이스 관리자에서 홈페이지 주소 등록
- 인스타그램 프로필 링크를 홈페이지 주소로 변경
- 에어비앤비 소개글에 홈페이지 주소 추가

---

## 앞으로 수정하는 방법

| 방법 | 설명 |
|---|---|
| GitHub 웹에서 직접 | 파일 클릭 → 연필 → 수정 → Commit → **자동 배포** |
| 파일 교체 | Add file → Upload files → 같은 이름으로 올리면 덮어씀 |
| Claude Code 연동 | 다음 단계. 명령 한 줄로 수정·배포 |

**커밋하면 Vercel이 알아서 다시 배포합니다.** 따로 누를 버튼이 없습니다.

---

## 아직 채워야 할 항목

| # | 항목 | 찾는 법 |
|---|---|---|
| 1 | 사업자정보 | 전 페이지 하단 — `[사업자정보 확인 필요]` |
| 2 | 주중·주말·성수기 구간별 요금 | `info.html` |
| 3 | 인원 추가 요금 기준 | `info.html` |
| 4 | 취소·환불 규정 | `info.html` |
| 5 | 조식 제공 여부·가격 | `info.html` |
| 6 | 반려동물 동반 가능 여부 | `info.html` |
| 7 | 흡연·파티 규정 | `info.html` |
| 8 | 네이버 지도 임베드 | `info.html` |
| 9 | 주변 관광지 3곳 | `info.html` |

파일에서 **`확인 필요`** 로 검색하시면 전부 나옵니다.

> **취소·환불 규정과 사업자정보는 법적으로 필요합니다.** 오픈 전에 반드시 채우십시오.

---

## 문제가 생기면

| 증상 | 원인과 해결 |
|---|---|
| 배포는 됐는데 화면이 깨짐 | `assets` 폴더가 통째로 안 올라갔을 가능성. 저장소에서 `assets/style.css` 가 보이는지 확인 |
| 사진이 안 뜸 | `assets/img` 안에 44개 파일이 다 있는지 확인 |
| 페이지 이동이 안 됨 | `vercel.json` 이 저장소 최상단에 있는지 확인 |
| 수정했는데 그대로 | Vercel의 Deployments 탭에서 최신 배포 상태 확인. 브라우저는 `Ctrl+Shift+R` 로 강력 새로고침 |

---

## 파일 구성

```
gangsanjae-deploy/
├─ index.html          메인
├─ space.html          공간
├─ healing.html        치유 프로그램
├─ info.html           이용안내 · 오시는 길
├─ 404.html            없는 페이지
├─ favicon.svg         브라우저 탭 아이콘
├─ robots.txt          검색로봇 안내
├─ sitemap.xml         사이트맵
├─ vercel.json         배포 설정 (캐시 · 보안 · 주소 형식)
├─ .gitignore          업로드 제외 목록
└─ assets/
   ├─ style.css        전체 디자인
   ├─ app.js           스크롤 효과 · 모바일 메뉴
   └─ img/             사진 44장
```
